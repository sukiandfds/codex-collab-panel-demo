# 开发已知问题记录：手机/Web 输入、引导、状态一致性与计时

记录日期：2026-07-25  
当前阶段：问题排查与记录，不实施功能修复  
适用页面：单人 Codex Web 页面；涉及群聊页面差异时单独标注

## 1. 本次用户反馈

用户在手机和电脑浏览器持续使用真实 Codex Web Demo 后发现：

1. 输入框不能发送图片；
2. Codex 正在运行时，输入框被禁用，无法补充指令或引导当前任务；
3. 手机浏览器和电脑浏览器显示的“Codex 正在运行”内容有时不一致；
4. 有时能看到 Codex 正在做什么，有时只有笼统的运行提示；
5. 运行计时会跨越多轮对话持续累计，没有按每一轮任务重新开始。

这些问题目前都属于真实功能缺口或状态模型缺陷，不是最终产品刻意保留的设计。

## 2. 排查结论摘要

| 问题 | 当前结论 | 状态 |
| --- | --- | --- |
| 图片发送 | 只有图片读取和展示，没有图片上传与发送协议 | 未实现 |
| 运行中继续输入 | 前端主动禁用输入；后端只有 `turn/start`，没有 `turn/steer` | 未实现 |
| 不同浏览器状态不一致 | 最新中文状态只存在于各浏览器本地内存，晚连接或断线设备会缺失 | 已定位 |
| 有时看不到具体工作 | Web 只能完整观察自己启动的 Turn；Desktop 启动的 Turn 不进入该事件通道 | 已定位 |
| 计时跨多轮累计 | 后端复用旧 `startedAt`，没有按 Turn 重置 | 已确认 Bug |

## 3. BUG-20260725-001：输入框不能发送图片

- 严重程度：P1，影响移动端真实使用
- 当前状态：未实现
- 用户表现：输入框只有文本区域和发送按钮，没有相册、拍照或文件选择入口

### 3.1 代码证据

前端 `Composer` 只保存一个字符串：

```text
text -> executionApi.sendMessage(threadId, text)
```

`executionApi` 只发送 JSON：

```json
{
  "threadId": "...",
  "text": "..."
}
```

后端 `/api/session/message` 也只读取 `body.text`，随后调用：

```text
turn/start
input: [{ type: "text", text }]
```

现有 `content-blocks.mjs` 和媒体服务只解决历史图片的读取、授权和显示，并不提供浏览器上传图片的路径。能够显示图片不等于能够发送图片。

### 3.2 正确修改方向

后续最小实现需要：

1. 输入框增加轻量图片选择入口，移动端允许相册或拍照；
2. 前端使用受控上传接口发送图片字节，而不是把手机本地路径当作字符串；
3. 后端限制图片类型、文件大小、像素尺寸和数量；
4. 图片保存到明确的临时或项目运行目录，不写入 Codex JSONL；
5. 调用当前 Codex 协议支持的图片输入类型，把文本和图片作为同一 Turn 的输入；
6. 任务完成或超过保留期后清理临时文件；
7. 群聊图片和单人 Codex 图片应共享安全媒体层，但保持各自消息模型。

### 3.3 验收标准

- 手机可从相册选择或直接拍摄一张图片；
- 发送前能看到缩略图并可移除；
- 图片和文字能进入同一个真实 Codex Turn；
- Codex 能读取图片内容并回复；
- 刷新页面后历史消息仍能正确显示该图片；
- 不暴露手机或电脑本地绝对路径；
- 非图片、超大文件和异常图片会得到清晰错误提示。

## 4. BUG-20260725-002：Codex 运行时无法继续输入或引导

- 严重程度：P1，直接影响长任务控制体验
- 当前状态：未实现
- 用户表现：Codex 开始工作后，输入框整体变灰，无法补充新信息、纠正方向或要求停止

### 4.1 代码证据

当前 `Composer` 的禁用条件包含：

```text
status.active
```

`useCodexExecution.sendMessage` 也在 `status.active` 时直接返回，不会发送请求。即使绕过前端，后端也只有 `turn/start`，没有运行中追加输入的接口。

当前尚未接入：

- `turn/steer`：向正在执行的 Turn 补充或调整指令；
- `turn/interrupt`：停止正在执行的 Turn；
- 当前活动 `turnId` 的可靠保存；
- 补充指令已经接收、等待处理或失败的状态。

### 4.2 正确修改方向

1. Codex 活跃时保持文本框可编辑；
2. 空闲时发送使用 `turn/start`；
3. 运行中发送使用 `turn/steer`，并关联当前 `turnId`；
4. 提供一个明确的停止按钮，调用 `turn/interrupt`；
5. 最新真人指令优先，但要区分“补充信息”和“推翻方向”；
6. 如果活动 Turn 已经结束，Steer 请求应安全降级为新的 Turn，不能丢失文字；
7. 多人使用时必须记录是谁补充或停止了任务，并检查控制权。

### 4.3 验收标准

- Codex 运行时输入框仍可输入；
- 用户补充一句话后，页面立即显示“补充指令已发送”；
- Codex 能在当前 Turn 中收到该内容；
- 用户可以停止当前执行；
- 任务完成和补充指令同时发生时不会丢消息或重复创建 Turn；
- 手机、电脑浏览器看到相同的补充和停止状态。

## 5. BUG-20260725-003：手机与电脑浏览器的运行状态不一致

- 严重程度：P1，用户无法判断 Agent 是否仍在工作
- 当前状态：已定位，尚未修复
- 用户表现：一个设备能看到具体中文工作说明，另一个设备只显示“Codex 正在运行”，或者两个设备显示的内容不同

### 5.1 直接原因一：中文状态只保存在浏览器本地

后端在一条 `commentary` 消息完成时广播：

```text
assistant_commentary
```

前端收到后只写入当前浏览器的 React 状态 `commentaryText`。服务端 `/api/execution-status` 只保存 `phase`、`label`、`detail` 和时间，不保存最新一段 `commentary`。

因此：

- 已经在线的设备可以收到该段文字；
- 稍后打开页面的设备无法补取之前的文字；
- SSE 短暂断线期间错过事件的设备不会自动恢复；
- 切换 Thread 后本地 `commentaryText` 会被清空。

### 5.2 直接原因二：不同运行实例的事件不可见

Web Demo 使用独立 app-server。只有通过这个 Web 服务启动的 Turn，才能完整产生并转发：

- `turn/started`；
- `item/started`；
- 完成后的中文 `commentary`；
- `turn/completed`。

如果任务由 Codex Desktop 自己的运行实例启动，Web 服务不会收到 Desktop 的实时 Notification。浏览器可能只能在任务完成后从持久化 Thread 读取最终消息，无法持续显示执行过程。

该问题与 `DEVELOPMENT_BUG_LOG_2026-07-24_DESKTOP_WEB_SYNC.md` 中记录的 Desktop/Web 运行实例分离属于同一架构边界。

### 5.3 直接原因三：不同页面使用不同状态模型

单人 Codex 页面使用 `execution-tracker` 的 Thread 状态；群聊页面使用 `group-room-store` 中的 Agent 状态和另一套 SSE 事件。两套页面当前尚未共享统一的 `AgentRun` 或状态快照，因此内容和更新频率也可能不同。

### 5.4 SSE 保活不是页面刷新

`realtime-hub.mjs` 每 20 秒发送一次：

```text
: keep-alive
```

这是 SSE 注释，只用于保持连接。浏览器 `onmessage` 不会收到它，也不会因此更新 React 页面。它不是每 20 秒刷新状态的原因。

### 5.5 正确修改方向

1. 服务端为每个 Thread 保存完整、可恢复的当前状态快照；
2. 快照至少包含当前 `turnId`、phase、最新完整中文状态、开始时间、更新时间和递增序号；
3. 新浏览器连接或断线重连时先读取快照，再接收增量事件；
4. 每条状态事件携带序号，客户端忽略过期事件；
5. 手机与电脑浏览器连接同一 Thread 时必须看到相同快照；
6. Desktop 发起的 Turn 在统一 Connector 完成前应明确显示“Desktop 端任务，实时过程暂不可见”，不能伪装成完整同步；
7. 后续把单人页面和群聊页面统一到 `Task / AgentRun / ActivityEvent` 状态模型。

### 5.6 验收标准

- 手机先打开、电脑后打开时，两端显示相同的最新状态；
- 电脑先打开、手机后打开时，手机能立即恢复当前状态；
- 任一设备断网重连后不需要手动刷新；
- 同一 Thread 的状态文字、阶段和开始时间一致；
- 页面能够明确区分 Web 启动的任务和 Desktop 启动但暂不可实时观察的任务。

## 6. BUG-20260725-004：运行计时没有按 Turn 重置

- 严重程度：P1，状态信息明显错误
- 当前状态：已确认代码缺陷
- 用户表现：“Codex 正在运行”的时间跨越多轮对话持续累计，而不是每次新任务从零开始

### 6.1 代码根因

`execution-tracker.mjs` 当前生成状态时使用：

```text
active ? previous.startedAt || now : previous.startedAt
```

上一轮完成后，状态对象仍保留旧 `startedAt`。下一轮 `markSubmitted` 或 `turn/started` 再次进入 active 时，会继续采用上一轮时间，因此计时跨 Turn 累计。

排查时读取当前 Thread 的状态接口，得到：

```text
phase: command
active: true
startedAt: 2026-07-24T15:50:57.386Z
updatedAt: 2026-07-24T16:02:14.838Z
```

该 `startedAt` 来自更早一轮执行，并非当前命令阶段单独开始的时间。

### 6.2 当前界面与数据层的差异

2026-07-24 的轻量状态改动已经从单人 Web 页面移除了每秒 React 计时器，因此新构建页面不再主动每秒重绘数字。但后端 `startedAt` 仍然错误：

- 旧页面缓存或其他状态组件仍可能显示累计时间；
- 后续重新增加耗时展示时会继续读到错误数据；
- 多设备恢复状态也会继承错误开始时间。

因此不能把“暂时不显示计时”当作计时问题已经修复。

### 6.3 正确修改方向

1. 状态必须按 `turnId` 管理，而不是只按 `threadId` 维护一个无限复用的开始时间；
2. 新 `markSubmitted` 或新的 `turn/started` 应创建新的 `startedAt`；
3. 同一个 Turn 内从分析、命令、文件修改到回复阶段保持同一个开始时间；
4. `turn/completed`、failed 或 interrupted 时记录 `completedAt` 并停止计时；
5. 页面断线重连后从服务端恢复当前 Turn 的开始时间；
6. 新 Turn 不得继承上一轮的 `startedAt`。

### 6.4 验收标准

- 每次发送新指令时计时从零开始；
- 同一 Turn 内切换执行阶段不会重置总耗时；
- 完成后计时停止；
- 下一轮任务重新从零开始；
- 手机和电脑浏览器显示相同耗时；
- 页面重连不会把任务开始时间改成重连时间。

## 7. 性能相关补充

当前单人页面在 2026-07-24 已停止逐字推送最终回复，并移除了每秒 React 计时器。中文过程说明只在一条完整 `commentary` 完成后推送一次。

仍然存在的周期行为包括：

- SSE 每 20 秒保活：只写一条注释，不触发页面状态更新；
- 群聊成员每 20 秒 Presence 心跳：只在群聊页面运行；
- JSONL 降级源每 60 秒兜底检查：运行在服务端，不等于浏览器刷新。

群聊页面仍然使用 `group_agent_delta` 逐字更新 Agent 回复，并在每次增量后自动滚动。这与单人页面的批量更新策略不同，未来如果手机群聊继续出现卡顿，需要单独记录和优化，不能与本次单人页面问题混为一谈。

## 8. 建议处理顺序

1. **先修计时归属**：改动小、原因明确，避免所有后续状态功能继续使用错误时间；
2. **再做运行中引导与停止**：补齐 `turnId`、`turn/steer` 和 `turn/interrupt`，直接改善手机控制体验；
3. **统一状态快照与重连**：让手机和电脑浏览器看到同一内容；
4. **实现图片发送**：需要上传、安全校验、临时文件和协议输入，工作量高于单纯增加一个按钮；
5. **最后统一群聊与单人状态模型**：在多 Agent 自动讨论前建立稳定的 `AgentRun` 事件基础。

## 9. 当前临时使用方式

在后续修复前：

- 图片只能作为已有对话附件被浏览器读取，不能从 Web 输入框发送；
- Codex 运行时不能从当前输入框补充指令，需要等待本轮完成；
- 手机和电脑浏览器应打开带同一 `thread` 参数的地址，否则可能选择不同任务；
- 某个设备没有显示最新中文状态时，不代表 Codex 已掉线，应同时查看连接提示和最终消息；
- 当前计时数据不应作为真实单轮耗时依据；
- Desktop 与 Web 同一 Thread 仍避免并发发送。

## 10. 相关文件

- `web-ui/src/components/Composer/Composer.tsx`
- `web-ui/src/features/execution/hooks/useCodexExecution.ts`
- `web-ui/src/features/execution/data/executionApi.ts`
- `web-ui/src/features/conversations/realtime/useConversationEvents.ts`
- `windows/server/request-handler.mjs`
- `windows/server/app-server-conversation-store.mjs`
- `windows/server/execution-tracker.mjs`
- `windows/server/realtime-hub.mjs`
- `DEVELOPMENT_BUG_LOG_2026-07-24_DESKTOP_WEB_SYNC.md`
- `项目战略与多角色评审/05-多人协作与多Agent技术路径调研报告-2026-07-22.md`

本文件只记录已知问题和建议修复方向。本轮没有修改功能代码，也没有声明上述能力已经完成。
