# 开发日志：2026-07-22

## 一、今天已经完成的内容

### 1. 真实 Codex 对话数据接入

- 新增 Codex `app-server` 客户端，通过 `thread/list` 和 `thread/read` 读取当前项目的真实任务。
- 会话标题优先使用 Codex 的 `thread.name`，不再把第一条用户消息误当作标题。
- 已确认当前任务的真实标题能够读取为“分析项目结构”。
- 保留 JSONL 读取作为降级方案，`app-server` 不可用时仍能展示项目对话。

### 2. 对话内容结构化渲染

- 不再把所有消息压成一段纯文本，而是按内容块传给前端。
- 支持 Markdown、代码块、列表、链接、表格和选项内容。
- 增加图片、音频、视频和普通文件的内容类型。
- 图片可在对话中直接显示；音频和视频使用浏览器原生播放器；其他文件提供打开或下载入口。
- 增加受控媒体文件接口，并支持 Range 请求，避免音视频必须一次性完整下载。

### 3. 长对话加载与性能优化

- 首次只加载最近 60 条消息，向上滚动时再加载更早历史。
- 使用虚拟列表渲染长对话，减少同时存在于页面中的 DOM 节点数量。
- 增加会话缓存，切换已打开过的任务时减少重复请求。
- 将反复递归扫描 JSONL 目录改为文件系统事件监听，并保留低频兜底校准。
- 当前项目仅聚焦一个项目目录内的真实对话，不扩展多人房间和跨项目功能。

### 4. 前后端职责拆分

后端已从单个大型脚本拆分为：

- `app-server-client.mjs`：Codex app-server 通信。
- `app-server-conversation-store.mjs`：app-server 会话数据源。
- `jsonl-conversation-store.mjs`：JSONL 降级数据源。
- `conversation-service.mjs`：数据源选择和统一会话接口。
- `content-blocks.mjs`：消息内容标准化。
- `media-service.mjs`：媒体文件安全读取和 Range 响应。
- `realtime-hub.mjs`：实时更新通知。
- `request-handler.mjs`：HTTP/API 路由。
- `static-files.mjs`：前端静态文件服务。

前端已按功能归属拆分到 `web-ui/src/features/conversations/`，分别负责请求、数据模型、实时连接、状态管理、内容渲染、会话列表和连接状态。UI 视觉文件与数据、功能逻辑保持分开，后续修改内容能力时不应再顺带改变 UI 样式。

### 5. 开发与启动入口

根目录已经提供统一的 pnpm 命令：

```powershell
pnpm build:ui
pnpm start:demo
pnpm start:demo:build
```

正式本地 Demo 入口统一为：

```text
http://127.0.0.1:9360/?token=demo123
```

### 6. 浏览器双向控制与执行状态

- 输入框已从只读展示升级为可向当前真实 Codex Thread 发送指令。
- 后端使用 `thread/resume -> turn/start` 继续原任务，不创建虚构会话。
- app-server Notification 已接入统一事件分发，页面可以显示提交、执行、完成和失败状态。
- Agent Message Delta 会通过 SSE 推送到页面，并在对话区显示流式回复。
- 执行能力按功能归属放在 `web-ui/src/features/execution/` 和 `windows/server/execution-tracker.mjs`，没有重新堆回 UI 页面组件。
- 用户已在电脑浏览器实际发送消息并获得真实回复，输入框显示“Codex 已完成”。

### 7. Codex 运行时兼容修复

- 首次浏览器和手机测试均出现“Codex 启动任务失败”。
- 已确认旧全局 `codex-cli 0.122.0` 无法恢复由 `0.145.0-alpha.18` 创建的 Desktop Thread。
- 后端现在优先使用 `CODEX_APP_SERVER_BIN`，否则扫描 Codex Desktop 管理的运行时并选择最高可运行版本；全局 npm 只作为最后兜底。
- 会话读取失败改为按请求回退 JSONL，不再因一次失败永久禁用整个服务的发送功能。
- 修复后 Demo 已重启，浏览器发送路径由用户实际验证成功。

## 二、开发过程与关键调整

1. 最初的 Demo 直接解析 JSONL，把第一条用户消息作为标题，把消息内容压成纯文本。
2. 接入真实数据后发现，标题、Markdown、选项和附件都需要保留 Codex 的结构化语义，不能继续依赖纯文本猜测。
3. 因此改为优先读取 Codex `app-server` 的结构化任务数据，JSONL 仅作为兼容降级。
4. 为避免数据接入继续影响页面外观，后端按数据源、内容、媒体、实时连接和路由拆分；前端按功能归属拆分。
5. 在真实长对话上增加分页、虚拟渲染和缓存，避免一次性加载与渲染全部历史。
6. 最后只完成 TypeScript、Node 语法、构建和接口级基本检查；按照用户要求，没有执行浏览器截图或视觉验收。

## 三、开发中遇到的问题及原因

### 1. 标题来源错误

最初使用第一条用户消息生成标题，是因为 JSONL 解析路径没有稳定读取 Codex 的任务名称。结果是侧栏显示对话正文，而不是 Codex 中已经存在的标题。现已改为优先使用 `thread.name`。

### 2. 内容渲染不正确

旧实现只返回 `message.text`，Markdown、代码、选项和附件结构在后端已经丢失，前端无法正确还原。现在改为统一内容块模型，并由对应渲染组件分别处理。

### 3. 数据接入时误改 UI

此前数据、功能和样式没有形成清晰边界，接入真实数据时同时调整了布局和状态展示，导致原有 Codex 复刻样式发生偏移。问题不在于必须修改 UI，而在于模块边界设计不足。当前代码已按功能拆分；今后 UI 文件只有在用户明确要求时才修改。

### 4. 早期组件拆分粒度不合适

最初只是按页面区域拆成大组件，没有按“请求、状态、实时连接、列表、内容类型、媒体”等功能所有权拆分。多人长期维护时仍容易互相影响。本轮已经转为按功能归属组织代码。

### 5. JSONL 扫描产生额外延迟

旧方案周期性递归遍历整个 sessions 目录，即使没有新对话也会重复做文件系统工作。本轮改为事件驱动监听，并让 app-server 成为主要数据源，减少重复扫描。

### 6. Codex app-server 资料与调用问题

- 在线读取官方手册时遇到 Windows Schannel TLS 握手失败，未能直接使用该请求结果。
- 随后改用本机 Codex 生成的 app-server schema，避免继续在网络问题上消耗时间。
- PowerShell 包装入口不能透明支持 app-server 的 stdio 通信，改为调用本机 Codex 的 Node 入口后正常工作。

### 7. PowerShell 编码与退出状态误判

- 内联中文断言被 PowerShell 编码破坏，后续改成与语言无关的结构和数量断言。
- 一次重启命令最后执行了匹配检查，匹配失败使整个命令显示失败，但服务实际上已经启动。后续把“启动”和“就绪检查”分开判断，不再用最后一条辅助命令的退出码代表服务状态。

### 8. 验收边界

本轮没有执行浏览器截图、像素对比或视觉验收。代码检查通过只表示语法、构建和接口基本可用，不代表页面样式已经获得用户验收。

### 9. 同一任务写入成功，但 Desktop 页面没有热刷新

浏览器测试成功后发现：消息和回复已经写入同一个 Thread `019f84c5-eb00-75d2-8726-fa8a11339e53`，Web API 也能立即读取，但当时已经打开的 Codex Desktop 任务页面没有显示这组外部追加内容。

当前判断：网页 Demo 启动的是独立 app-server 进程。它与 Desktop 共享持久化 Thread，但不共享 Desktop 当前页面的内存状态和实时事件通道。因此这不是“写入了另一条会话”，而是“双端持久化一致、Desktop 实时视图未同步”。在完成统一连接或 Desktop 同步机制前，不建议网页和 Desktop 同时向同一个 Thread 发送指令。

### 10. 调试过程造成的本机环境问题

排查兼容性时错误执行过一次被中断的全局 npm Codex 升级，留下不完整包、npm 备份目录、命令 shim 备份和少量临时目录。这没有污染项目的 `package.json` 或 pnpm lockfile，但当前全局 `codex` 命令状态不稳定。

相关停止进程、删除目录和恢复 shim 尚未执行，因为属于本机全局环境修改，必须单独确认。详细证据和精确路径见 `DEVELOPMENT_BUG_LOG_2026-07-22.md`。

## 四、已完成的基本检查

- 后端所有 `.mjs` 文件通过 `node --check`。
- TypeScript 编译通过。
- Vite 生产构建通过。
- `git diff --check` 通过。
- 项目接口返回 HTTP 200。
- API 能读取当前项目的 8 个任务。
- 当前任务标题返回“分析项目结构”。
- 分页检查能从总计 52 条消息中按请求返回 10 条。
- 修复后服务在 `9360` 重新启动，项目接口返回 `200 application/json`。
- 用户在电脑浏览器发送指令并收到真实回复，状态显示完成。
- Web API 在原 Thread 中读取到浏览器消息和对应回复，确认没有生成平行假会话。

## 五、之后的方向

按依赖关系和优先级推进：

1. **明确双端同步边界**：验证切换或重新打开 Desktop Thread 是否会重新载入外部记录；调研是否能连接 Desktop 自身的 daemon/proxy/IPC。不要通过直接改 JSONL 或伪造 Desktop UI 解决。
2. **避免双端并发写入**：统一 Connector 之前，同一 Thread 同一时间只由网页或 Desktop 一端发送。
3. **正式远程访问安全**：用可靠认证替代固定 `demo123`，默认收紧监听范围，保留审批和沙箱，不把当前局域网 Demo 当作正式远程执行入口。
4. **恢复本机全局 Codex 环境**：在用户单独确认后，精确清理本轮 npm 残留，不删除其他历史缓存。
5. **真实附件与内容稳定性**：继续验证图片、音视频、文件和未知内容类型，但不顺带修改 UI。
6. **多人协作**：单项目 Connector、同步、安全和日志边界稳定后，再进入成员、项目群、项目经理 Agent 和多 Agent 组织层。

## 六、本次 Git 提交范围

本次提交包含真实对话数据接入、内容渲染、性能优化、功能拆分、浏览器发送、执行状态、流式回复、Codex 运行时兼容修复、战略调研、开发问题记录和本日志。

本地依赖、pnpm store、浏览器 profile、临时文件、构建产物和 TypeScript 生成文件不会提交到 GitHub。今天整理完成的愿景、调研和历史原型会作为项目决策依据一并提交，但不把它们误写成已经实现的功能。
