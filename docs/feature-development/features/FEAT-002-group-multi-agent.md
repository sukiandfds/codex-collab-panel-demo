---
feature_id: FEAT-002
title: 项目群聊与多 Agent 协作
status: in_progress
current_version: v0.4.2
last_updated: 2026-08-07 18:36 +08:00
owners: [group_chat, multi_agent]
key_paths:
  - web-ui/src/features/group-chat
  - windows/server/group-room-store.mjs
  - windows/server/multi-agent-service.mjs
---

# FEAT-002：项目群聊与多 Agent 协作

## 当前快照

- 项目群有真实成员、持久化群消息和四个真实 Codex Agent：项目经理、研究、开发、审查。
- 用户可以在讨论/开发模式中发送消息、选择或 `@` 一个或多个 Agent。
- Agent 收到必要群聊上下文后按有限轮次回复，并能在回复中邀请其他 Agent 继续讨论。
- 页面显示 Agent 当前阶段、增量回复、在线成员和附件。
- 当前不是无限自主 Agent 社会，也没有正式账号、权限、持久化任务队列和冲突仲裁。
- 当前 v0.4.1 是可运行原型，不代表目标协作底座已经完成；正式 Task、AgentRun、ActivityEvent 和审批账本仍待开发。
- 初版重点是一台电脑上的多个模型和多个 Agent 协作，不先做多电脑自动调度，也不把 Orca 作为底座依赖。
- M1 已加入客户端消息 ID、服务端消息顺序、请求重试幂等和前端临时/正式消息对账；代码与构建检查通过，服务端尚未重启加载和真实设备验收。
- v0.4.2 已将群聊侧栏、顶部、输入区、加载反馈和移动端布局调整为单人页的界面语言，同时保留成员、Agent、`@`、讨论/开发模式和成果审核；本轮没有修改单人页文件。
- 消息整体样式、富文本、消息操作、历史消息、流式回复、指令等候队列、模型与上下文明确暂缓，继续作为本功能的待开发项目。

## 用户可见结果

用户从一个群聊入口发送消息。未点名时，项目经理判断自己直接回答、创建 Worker 任务或请求补充信息；明确 `@` 时由目标 Agent 直接响应。被安排的 Agent 在群里汇报进度、结论、交付物、风险和待确认事项。当前原型已经能展示部分 Agent 回复，但正式任务账本、可恢复队列、统一汇报和审批状态尚未完成。

## 目标与边界

目标：在单人 Codex Web 的消息、实时事件、附件、断线恢复、停止/追加和失败反馈标准之上，建立可靠的单机多 Agent 项目协作中心。群聊不是另一套质量更低的对话系统。

当前不解决：

- 无限自动讨论或无限 Token 使用；
- 多项目、多租户、企业权限与正式成员账号；
- 多电脑之间自动抢任务、远程自动执行和设备调度；
- Orca 的整体迁移、Fork 或产品层接入；
- 把群聊状态强行合并进单人 Turn 状态；
- 保证群聊完整上下文出现在 Codex Desktop 当前页面。

## 架构与数据链路

```text
GroupApp / useGroupRoom
  -> /api/group/*
  -> group-room-store (房间、成员、消息)
  -> multi-agent-service (目标 Agent、上下文、有限轮次)
  -> Codex app-server threads
  -> realtime-hub /events
  -> Agent 状态、增量文本、群消息
```

单人页的 `threadId/turnId` 状态和群聊的房间/Agent 队列属于不同功能域。目前只共享稳定的附件、实时连接和导航 UI。

## 开发计划与关键决策

- 先完成一个真实项目群，不扩展多个群和组织管理。
- 默认由项目经理 Agent 接收未点名消息；明确 `@` 时按目标 Agent 调度。
- 先建立消息幂等、房间事件顺序和可恢复任务账本，再增加项目经理分派和更多模型。
- 每个路由决定、Task、AgentRun、进度事件和最终结果都要能追溯到原始群消息。
- 上下文只包含完成当前讨论所需的群消息和角色责任，限制轮次避免 Token 无边界增长。
- Worker 只接收任务摘要、关联文件、已有决策、验收标准和必要证据；完整过程单独记录。
- Agent 完成后在群内汇报，项目经理按需汇总；没有新增价值时不重复回答。
- 停止、失败、重试、等待用户、待审批和部分完成必须具有明确状态。
- 真实任务与测试分开，自动验证不向群历史发送污染消息。
- 单人和群聊可以有不同运行对象，但应共用已经验证的消息呈现、过程事件、附件、断线恢复和错误反馈标准。
- 项目经理分工、多 Agent 讨论、审查和任务队列属于群聊特有能力，按 M1-M9 阶段逐项实现；不能用这些特性替代基础能力对齐。
- 长期采用模型无关的 Agent/Provider 边界：职位、模型供应商、模型和工具能力分别配置，不把“项目经理/研究/创意/开发”等角色写死为 Codex。
- 不默认把每条消息广播给所有模型；由项目经理或明确 `@` 路由到必要角色，共享提炼后的任务上下文，控制 Token、费用和重复输出。
- 外部模型接入前必须验证 API 能力与网页版体验是否一致；不能因为豆包网页版检索强或 Grok 更大胆，就直接假定其公开 API 具备相同检索、引用和上下文能力。
- 采用分层 Agent：项目经理使用高能力模型负责澄清、拆解、冲突处理和结果审核；边界明确的执行任务交给更快、更低成本的专业 Worker；审查 Agent 只在风险或质量要求需要时调用。
- Worker 只接收任务摘要、关联文件、已有决策、验收标准和必要证据，不复制完整群聊；返回结果摘要、产物位置、风险和待决问题，完整过程单独留档。
- “使用子 Agent”不自动等于节省 Token；只有精准路由、限制轮次、限制上下文并使用合适成本模型时才可能降低总成本。

### 2026-08-06 | 初版单机多 Agent 路由规则

- 当前主场景是一台电脑上的多个模型和多个 Agent，不根据真人在线人数改变路由规则。
- 用户未明确指定时，默认由项目经理 Agent 接收并判断：直接回答、创建 Worker 任务或请求补充信息。
- 用户明确 `@` 某个 Agent 时，由目标 Agent 直接响应；项目经理只在需要汇总、冲突处理或用户要求时介入。
- 项目经理分发任务后，各执行 Agent 需要在群内提供进度，不能只在后台执行后一次性返回结果。
- 用户可以针对某条 Agent 对话继续追问，后续消息应保留与原对话的关联。
- 多电脑协作和 Orca 接入暂不进入初版，只保留未来 Runtime Adapter 扩展边界。
- 以上是已确认的产品方向，不代表当前代码已经实现；消息账本、任务恢复和统一汇报仍待开发。

## 问题记录

| 问题编号 | 分类 | 状态 | 问题 | 根因与正确路径 |
| --- | --- | --- | --- | --- |
| `FEAT-002-I01` | 特例 | resolved | 早期页面只是静态原型，用户发言后没有真实回复 | 没有连接 Agent 调度；后续接入真实 app-server 和 multi-agent service |
| `FEAT-002-I02` | 特例 | resolved | 商讨模式无人回复 | 发送模式和默认 Agent 调度不完整；未点名时现在回退到项目经理 |
| `FEAT-002-I03` | 特例 | mitigated | `@` 菜单可能与中文输入法组合态冲突 | 键盘行为必须检查 `isComposing`，仍需真实手机输入体验确认 |
| `FEAT-002-I04` | 特例 | active | 服务重启会丢失未完成讨论队列 | 当前队列在内存；正式版需要持久化 Job/Run 状态 |
| `FEAT-002-I05` | 普适 | active | 单人页和群聊看似相近但状态模型不同 | 见 `PROC-011`，只共享稳定组件，不强行共享运行对象 |
| `FEAT-002-I06` | 特例 | planned | 群聊的基础对话体验没有逐项与单人 Codex 对齐，导致过程停更、断线恢复、停止/追加和失败反馈可能各自实现 | 先建立基础能力对照表并复用稳定事件与 UI；群聊特有功能另行定义，不混在基础补齐中 |
| `FEAT-002-I07` | 架构方向 | planned | 后续需要接入不同模型，让各自优势参与讨论、执行和相互审查 | 建立模型无关的 Agent/Provider 层；角色与模型解耦，按任务路由并共享提炼上下文。优先评估豆包真实信息检索和 Grok 创意/反方能力的 API 可用性、引用质量、成本与稳定性 |
| `FEAT-002-I08` | 调研待确认 | planned | 外部案例声称可在 `~/.codex/agents/` 创建绑定 `gpt-5.6-luna` 的 Worker；本机搜索未发现 Luna 后曾被错误表述为“当前模型目录没有 Luna” | 本机 `codex-cli 0.130.0-alpha.5` 的 `multi_agent` 为 stable，但桌面端、CLI、自定义供应商可能使用不同模型来源；“未在当前配置中搜到”不等于“不存在或不支持”。实施前必须按当时 Codex 版本核对 Agent 文件位置、TOML schema、模型别名来源和真实调用结果 |
| `FEAT-002-I09` | UI 待开发 | planned | 群聊消息整体样式尚未迁移为单人页样式 | 先确认群聊发送者标识和多 Agent 信息如何保留，再统一消息宽度、间距和视觉层级；本轮不修改消息时间线 |
| `FEAT-002-I10` | UI 待开发 | planned | 群聊富文本显示仍使用独立 Markdown 渲染，未具备单人页的完整内容块能力 | 后续评估复用稳定内容渲染组件，同时保留群聊 Artifact 和附件挂载位置 |
| `FEAT-002-I11` | 功能待开发 | planned | 群聊消息缺少复制、编辑、分支、重新确认等单条消息操作 | 复制可以直接对齐；编辑、分支和重试必须先确认群聊服务端语义，不能只添加无效按钮 |
| `FEAT-002-I12` | 体验待开发 | planned | 群聊没有单人页的历史分页、长列表和阅读位置保持能力 | 后续单独设计群聊历史加载，不直接搬用单人 Turn 的滚动状态，避免影响单聊稳定性 |
| `FEAT-002-I13` | 体验待开发 | planned | 多 Agent 流式回复尚未与单人页的过程展示和最终回复收敛标准统一 | 保留多 Agent 并行能力，再统一临时回复、最终消息、失败和停止时的用户可见状态 |
| `FEAT-002-I14` | 功能待开发 | planned | 群聊没有“指令等候中”队列 | 先明确队列属于整个群、某个 Agent 还是具体任务，再设计直接发送、编辑、删除、排序和失败恢复 |
| `FEAT-002-I15` | 功能待开发 | planned | 群聊没有模型、推理强度和上下文控制 | 后续按 Agent/Provider 维度设计，不能直接照搬单聊的全局模型与上下文设置 |

## 群聊 UI 待开发清单

| 项目 | 当前状态 | 本轮边界 |
| --- | --- | --- |
| 消息整体样式 | planned | 本轮不改 `MessageTimeline` |
| 富文本显示 | planned | 本轮不替换群聊 Markdown 和 Artifact 渲染 |
| 消息操作 | planned | 本轮不增加复制、编辑、分支或重试 |
| 历史消息 | planned | 本轮不迁移分页、虚拟列表和滚动状态 |
| 流式回复 | planned | 本轮不修改多 Agent 流式与最终消息逻辑 |
| 指令等候队列 | planned | 本轮不定义群队列或 Agent 队列 |
| 模型与上下文 | planned | 本轮不增加群聊模型和上下文控制 |

## 版本时间线

### 2026-07-24 00:44 +08:00 | v0.1.0 | implemented_pending_review

- 计划：快速做出可真实使用的项目群和多 Agent 雏形。
- 实际：完成独立群聊页、成员、群消息、四个 Agent、讨论/开发模式和 `@` 提及。
- 偏差：首版调度仍不完整，出现“没人回复”。
- 问题：`FEAT-002-I01`、`FEAT-002-I02`、`FEAT-002-I03`。
- 验证：构建和接口通过；见 `DEVELOPMENT_LOG_2026-07-24.md`。
- Git：`74bfa88`。

### 2026-07-25 00:55 +08:00 | v0.2.0 | implemented_pending_review

- 计划：让多个 Agent 真正共享必要上下文并进行有限轮次讨论。
- 实际：支持同时点名多个 Agent、按顺序执行、上下文提示和 Agent 邀请其他 Agent。
- 偏差：仍保留单内存队列，没有扩展正式组织系统。
- 问题：`FEAT-002-I04`。
- 验证：multi-agent 定向测试和 `pnpm build:ui` 通过。
- Git：`f26e22b`。

### 2026-07-25 22:49 +08:00 | v0.3.0 | implemented_pending_review

- 计划：补足群聊附件、消息反馈、过程动画、移动端和实时体验。
- 实际：增加附件、增量展示、新消息提示、日期分隔、移动端 `@` 菜单和更明确的 Agent 状态。
- 偏差：正式排队、取消、重试和权限仍未进入本轮。
- 问题：`FEAT-002-I03` mitigated；`FEAT-002-I04` active。
- 验证：`pnpm build:ui` 和定向测试通过。
- Git：`5a8ad71`。

### 2026-07-28 10:04 +08:00 | v0.3.0 | backlog_updated

- 产品方向：多 Agent 不应绑定单一模型；不同模型按能力担任合适职位并参与讨论、执行和相互审查。
- 示例定位：Codex 负责代码执行；豆包候选用于真实信息检索；Grok 候选用于大胆创意、方向扩展和反方挑战。实际职责以 API 能力评估结果为准。
- 架构约束：角色、模型供应商、具体模型和工具能力解耦；上下文以任务摘要、关键证据和当前决策共享，不复制全部垃圾信息。
- 成本约束：不自动召集全部模型；由任务路由或用户 `@` 精确调用，限制讨论轮次并记录每个 Agent 的来源模型。
- 当前边界：新增 `FEAT-002-I07` 作为长期方向，不在单人 Codex 基础问题修复前开发接入。

### 2026-07-28 15:47 +08:00 | v0.4.0 | backlog_updated

- 用户确认：群聊部分特有功能仍需定义，但基本功能必须与单人 Codex 部分看齐。
- 新增 `FEAT-002-I06`，把消息、过程、附件、断线恢复、停止/追加和失败反馈列为基础对齐范围。
- 本轮只更新需求，没有修改群聊代码。

### 2026-08-03 10:00 +08:00 | v0.4.1 | direction_updated

- 用户确认：分层 Agent 符合产品设想，应作为群聊与多 Agent 调度的重要方向。
- 角色分层：高能力项目经理负责沟通、拆解、协调和验收；低成本专业 Worker 处理范围明确、可独立完成的任务；审查角色按风险触发。
- 上下文分层：Worker 获取提炼后的任务包而非完整对话，结果以摘要、产物、风险和待决问题回传，完整日志单独保存。
- 成本边界：子 Agent 本身会产生额外调用；必须通过精准路由、上下文裁剪、有限轮次和模型分级控制总 Token 与费用。
- Luna 核对：本机当前可检索配置没有发现 `luna`，但这不能证明模型不存在；截图中的 `gpt-5.6-luna` 可能来自另一版本、自定义目录或供应商别名。正式配置前需要源码/官方 schema 与真实调用验证。
- 本轮只记录产品与技术方向，没有创建 Agent 配置或修改群聊代码。

### 2026-08-06 22:48 +08:00 | v0.4.1 | direction_updated

- 目标收敛为单机多模型、多 Agent 协作地基：项目经理默认接待，`@Agent` 明确指定，Worker 在群内汇报。
- 开发顺序调整为先做消息幂等/顺序和 Task、AgentRun、ActivityEvent 账本，再做路由、Worker 任务包、汇总、控制和 Provider 扩展。
- Orca 暂不作为初版底座；多电脑只保留未来扩展可能，不进入当前主链路。
- 本轮只同步产品方向，没有修改群聊代码。

### 2026-08-06 22:59 +08:00 | v0.4.1 | in_progress

- M1 部分实现：增加 `clientMessageId`、持久消息 `sequence`、服务端请求重试幂等和前端乐观消息对账。
- 用户发送文本后可立即在群内看到消息并清空输入框；正式 SSE/接口消息到达后替换临时消息，不再长期保留两份。
- 修复附件消息恢复边界：只有附件、没有文字的群消息在服务重启后仍会保留。
- 定向服务端测试 8/8、Node 语法检查、`pnpm build:ui` 和 `git diff --check` 通过。
- 尚未重启服务或进行手机/多网页真实验收，不能把 M1 标记为完成。

### 2026-08-07 18:36 +08:00 | v0.4.2 | implemented_pending_review

- 计划：先让群聊的基础界面与单人页保持一致，不改动单人页和消息时间线。
- 实际：统一群聊侧栏品牌与列表层级、顶部标题和身份入口、输入框尺寸与发送按钮、连接加载与失败刷新提示、手机安全区和底部工具布局。
- 保留：在线成员、Agent 列表与状态、身份设置、`@`、讨论/开发模式、附件和成果审核。
- 暂缓：消息整体样式、富文本、消息操作、历史消息、流式回复、指令等候队列、模型与上下文，登记为 `FEAT-002-I09` 至 `FEAT-002-I15`。
- 验证：只进行 `git diff --check` 和 TypeScript `--noEmit` 基础检查；没有构建、启动服务或视觉验收。

## 下一步

- M0：以项目管理表单确认初版范围和验收场景。
- M1：受控重启后验收消息即时显示、幂等、顺序和重连行为；未通过前不进入完成状态。
- UI-P1：体验本轮侧栏、顶部、输入区、加载反馈和手机布局；根据真实结果修正，不顺带修改消息时间线。
- UI-P2：按 `FEAT-002-I09` 至 `FEAT-002-I15` 逐项确认产品语义后开发，不一次性强行迁移全部单聊能力。
- M2：将内存队列升级为可恢复的 Task、AgentRun、ActivityEvent 状态链。
- M3-M7：依次完成项目经理路由、`@Agent`、Worker 任务包、群内汇报、汇总、停止/重试/审批。
- M8：在核心协作链稳定后，再验证最小 Runtime/Provider 接口和一个外部 API 模型。
- M9：用真实开发、调研、审查和失败恢复场景验收，再同步最终文档和项目表单。
- `FEAT-002-I08` 的 Luna schema、模型来源和真实调用验证，放在 M8，不作为当前开发前置阻塞。
