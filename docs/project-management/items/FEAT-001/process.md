---
document_type: project_management_process
schema_version: 1
item_id: FEAT-001
last_updated: 2026-08-04 09:55 +08:00
---

# FEAT-001：单人 Codex Web 对话与控制过程记录

本文件保留 FEAT-001 独有的过程索引和本功能影响，不复制功能档案或通用问题台账中的完整正文。完整功能记录见 `docs/feature-development/features/FEAT-001-single-codex-web.md`；跨功能规则按 `PROC-*` 读取。

### 2026-07-22 | FEAT-001-P01 | detour | P1 | resolved

- 条目：FEAT-001
- 用户可见现象：会话列表标题曾显示首条正文或关键词概括，而不是 Codex 侧栏里的真实标题。
- 用户原话：`Codex里显示的就是标题，为什么你不显示标题而显示内容？`
- 助手初步理解：标题必须读取 Codex 的标题字段；找不到时只能明确显示降级状态，不能把正文伪装成标题。
- 错误路径：没有先确认 `thread.name`，直接从 JSONL 正文推断标题。
- 影响：用户无法判断是否打开了同一个对话，历史定位失真。
- 正确路径/决策：优先使用 app-server 的 `thread.name`，JSONL 只作兼容降级；问题详情仍由 `FEAT-001-I01` 维护。
- 防再犯触发器：任何“标题不对”反馈先检查真实元数据字段和降级链路，不改文案猜测规则。
- 归属判断：功能特例；不能升级为通用 `PROC-*`。
- 证据：`FEAT-001-single-codex-web.md` 的 `FEAT-001-I01`、`AI_ASSISTANT_WORK_RULES.md` 当前数据链路说明。
- 关联：`FEAT-001-I01`。

### 2026-07-22 | FEAT-001-P02 | detour | P1 | resolved

- 条目：FEAT-001
- 用户可见现象：Markdown、代码、表格、选项和附件内容在前端曾被压成普通文本，显示结构与 Codex 不一致。
- 用户原话：`内容渲染有些不对。`
- 助手初步理解：要保留真实内容结构，再由对应渲染组件展示，而不是只修一段文本样式。
- 错误路径：后端只返回 `message.text`，到前端后再猜测内容类型。
- 影响：代码、表格和媒体语义丢失，后续增加文件类型时反复返工。
- 正确路径/决策：统一输出内容块，保留 Markdown、代码、媒体和文件元数据；具体问题仍在功能档案中维护。
- 防再犯触发器：新增内容类型时先检查数据模型是否保留结构，再决定渲染器；禁止用字符串拼接代替内容块。
- 归属判断：功能特例；内容块和 UI 边界同时受 `PROC-001` 约束。
- 证据：`AI_ASSISTANT_WORK_RULES.md` 的 12.2、`docs/feature-development/features/FEAT-001-single-codex-web.md`。
- 关联：`FEAT-001-I05`、`FEAT-003`、`PROC-001`。

### 2026-08-01 | FEAT-001-P03 | validation | P0 | active

- 条目：FEAT-001
- 用户可见现象：跨设备任务结束后页面仍显示处理中；流式文字接近结束时短暂消失；新 Turn 开始时短暂出现上一 Turn 的过程。
- 用户原话：`手机完全看不到实时进度，全靠猜。`
- 助手初步理解：必须先确认当前展示属于哪个 Run/Turn，再决定是否补拉或更新，不能靠增加轮询和状态文案掩盖顺序问题。
- 错误路径：只验证实时消息能到达，没有把终态事件、恢复快照、流式缓冲和下一 Turn 的交接放在同一条事件顺序里验证。
- 影响：用户无法判断任务是否完成，出现“丢回复”和旧过程串入的体感。
- 正确路径/决策：该问题已经升级为跨功能 `PROC-022`；后续以 `runId/turnId/eventSeq` 建立单调顺序，终态和持久化快照优先，流式内容只追加。
- 防再犯触发器：出现文字消失、重开仍处理中或旧过程串入任一现象时，先保存同一 Turn 的事件序号、快照和终止事件，再修改。
- 归属判断：已从功能特例升级为普适问题；本文件只保留 FEAT-001 的用户影响。
- 证据：`docs/feature-development/DEVELOPMENT_COMMON_MISTAKES.md#PROC-022`、`FEAT-001-I24`。
- 关联：`FEAT-001-I08`、`FEAT-001-I15`、`FEAT-001-I17`、`FEAT-001-I24`、`PROC-022`。

### 2026-07-29 | FEAT-001-P04 | blocker | P0 | active

- 条目：FEAT-001
- 用户可见现象：任务和项目服务进入半失效或自重启中断状态，网页长时间显示处理中，之后核心接口也无法响应。
- 用户原话：`一直卡在回复中的情况，实际重新打开启动后，确实是回复完了。`
- 助手初步理解：服务在线不等于任务健康，恢复动作必须脱离承载请求的服务进程，且不能把停止和启动串在同一条在线任务里。
- 错误路径：用端口在线或本地 tracker 更新时间代替真实业务健康；曾在服务进程内直接执行 Stop/Start。
- 影响：网页和 Codex 状态混乱，可能让任务中断并扩大故障范围。
- 正确路径/决策：完整根因和恢复边界集中维护在 `PROC-019`、`PROC-020`；FEAT-001 只引用，不复制运行手册。
- 防再犯触发器：出现“端口在线但核心接口超时”或需要重启当前服务时，先执行真实业务健康检查并使用脱离服务进程的恢复入口。
- 归属判断：跨功能 P0；完整正文以 `PROC-*` 为准。
- 证据：`docs/feature-development/DEVELOPMENT_COMMON_MISTAKES.md#PROC-019`、`#PROC-020`、`FEAT-001-I12`。
- 关联：`FEAT-001-I12`、`PROC-019`、`PROC-020`。

### 2026-08-04 | FEAT-001-P05 | regression | P0 | active

- 条目：FEAT-001
- 产品基线：`63e8f948b8058d8236e7a4672b5c1f8bdbf8eb70`
- 记录边界：以下内容是用户在本次真实手机使用中连续发送的原文，逐字保存；助手分析和根因推测不得冒充用户原话。

#### 用户原文（逐字记录）

反馈一：

```text
现在我感受最深的两个问题就是：
1. 就是图片中这样子，上一个回答，前面一条信息变成了一条很久之前的信息，我不知道这是怎么回事。另外一个问题就是，我现在发送一条信息之后，我就看不到我发送的这条信息，而只能等待助手回复之后，我才能看到我刚才发送的问题。这很严重，因为最开始我比较满意的就是，我发送信息，我立刻能在我其他的网页版的设备上看到我刚发送的信息。结果你却把这个东西做没了，这让我很不满意。你把我让你参考的可用的功能，给现在给做没了，你这是什么意思？
```

反馈二：

```text
还有一个问题，现在助手给出回复之后，结果最后还有一个光标一直在闪
```

反馈三：

```text
我又发现一个问题，同一个答案，助手给出的回复，我这里看到显示了两次。
```

记录要求：

```text
把我说的问题的原文，我发送给你的信息的原文记录下来。
```

- 截图证据：`runtime/uploads/61c5a17451b53379c463a09f0e7845646c989efd0333fe762b9b661fde337782__Screenshot_2026-08-04-09-12-11-142_com.microsoft.emmx.jpg`
- 用户可见现象：消息顺序被旧内容替换；新发送的用户消息无法立即在当前及其他 Web 设备看到；最终回复结束后流式光标持续闪烁；同一助手答案显示两次。
- 影响：破坏此前已被用户认可的跨 Web 设备即时同步基线，用户无法确认发送是否成功，并可能看到错误顺序或重复内容。
- 当前状态：P0 回归已由用户实机确认；v0.10.9 已完成源代码修复并通过 Windows 测试 `84/84`、UI 生产构建、语法和补丁检查。按用户要求未重启服务，当前运行后端尚未加载修复，真实跨设备体验待验收。
- 关联：`FEAT-001-I24`、`FEAT-001-I25`、`PROC-022`、产品提交 `63e8f948b8058d8236e7a4672b5c1f8bdbf8eb70`。
