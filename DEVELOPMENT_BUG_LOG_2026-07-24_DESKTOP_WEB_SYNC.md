# 技术问题记录：Codex Desktop 与手机/Web 对话不同步

记录日期：2026-07-24  
问题编号：BUG-20260724-001  
当前状态：已确认 Desktop 当前页面不热刷新；Desktop 完全关闭后重新打开任务能否读取最新记录仍待实测  
严重程度：P1，影响跨设备连续使用；进入正式多人协作前必须明确解决或形成稳定交接机制

## 1. 用户发现的问题

用户通过手机浏览器进入单项目 Codex Web 页面，并在电脑端 Codex Desktop 关闭后继续发送消息。手机端仍能正常收到 Codex 的状态和回复，说明网页后台可以独立运行。

随后用户提出更关键的连续使用问题：

- 手机或 Web 页面产生的新消息，在电脑端已经打开的 Codex 对话中看不到；
- 手机或多人项目群可能已经形成大量新讨论和决定；
- 用户回到电脑前，希望在 Codex Desktop 继续同一项工作；
- 如果 Desktop 无法看到最新任务上下文，用户需要重新解释之前的内容，跨设备体验会被割裂。

这不是单纯的页面显示问题。它会影响任务上下文、最新指令、控制权和多人协作的一致性。

## 2. 当前系统结构

当前 Demo 的实际路径是：

```text
手机或电脑浏览器
        |
        v
本地 Node Web 服务（9360）
        |
        v
独立 Codex app-server 进程
        |
        v
Codex 持久化 Thread

Codex Desktop
        |
        v
Desktop 自己管理的运行进程、内存状态和事件订阅
```

Web 服务与 Codex Desktop 可以引用同一个持久化 Thread，但它们不是同一个正在运行的 app-server 实例，也不共享同一条实时 Notification 通道。

## 3. 已验证事实

1. 手机浏览器能够通过 `9360` Web 服务向真实 Codex Thread 发送消息。
2. Web 页面可以接收真实执行状态和 Codex 回复。
3. Web API 曾在 Thread `019f84c5-eb00-75d2-8726-fa8a11339e53` 中读取到浏览器发送的消息及对应回复，没有创建一条虚构会话。
4. Codex Desktop 已经打开的同一任务页面不会自动显示独立 app-server 新增的消息。
5. 用户关闭 Codex Desktop 界面后，手机 Web 对话仍可继续，说明当前 Web 控制链路不依赖 Desktop 窗口保持打开。
6. 当前 Demo 服务和 Desktop 使用各自的实时状态来源，因此“持久化记录指向同一 Thread”不等于“双端画面实时一致”。

## 4. 尚未验证的部分

以下内容不能先写成已经支持：

1. Codex Desktop 完全退出并重新启动后，是否一定会重新读取 Web 追加的最新 Thread 内容；
2. 只切换到其他任务再切回，是否会触发稳定刷新；
3. Desktop 是否存在可供外部连接的 daemon、proxy、IPC 或统一事件入口；
4. Desktop 对外部 app-server 写入的 Thread 是否存在缓存、索引延迟或版本兼容差异；
5. Desktop 与 Web 同时发送消息时，Turn 顺序、状态和审批请求能否保持一致；
6. 多人群讨论形成的新决定应以什么粒度进入个人 Codex Thread。

下一次最小实测是：在 Desktop 完全关闭期间通过手机完成一轮对话，然后重新启动 Desktop，打开同一个任务，核对最新用户消息、Codex 回复和消息顺序。

## 5. 根因判断

当前直接原因是运行实例和实时事件通道分离：

- Web Demo 启动独立 app-server；
- Desktop 使用自己管理的运行实例；
- Web 侧事件只推送给 Web SSE 客户端；
- Desktop 当前页面不订阅外部 app-server 的 Notification；
- Desktop 当前页面也没有被证实会监视持久化 Thread 的外部变化并自动重载。

这属于当前快速 Demo 的架构限制，不是最终产品应保留的设计目标。

## 6. 用户影响

### 6.1 跨设备连续使用

用户在手机上完成多轮沟通后回到电脑，可能无法直接在 Desktop 看到最新内容，需要重复说明上下文。

### 6.2 信息版本不一致

Web 已经形成新决定，但 Desktop 仍显示旧任务状态，容易让用户或 Agent 按过期要求继续工作。

### 6.3 双端并发风险

Web 与 Desktop 同时向同一个 Thread 发送消息时，两端看到的状态可能不同，消息顺序、审批和当前 Turn 归属也可能混乱。

### 6.4 多人协作放大问题

未来项目群可能包含大量真人和 Agent 消息。不能把全部群聊原样塞进每个 Codex Thread，但已经确认的决定和任务变化又必须可靠传递，否则项目群与实际执行会脱节。

## 7. 正确的产品边界

允许存在的差异：

- 项目群中的闲聊、旁支意见和不相关 Agent 日志不必全部出现在个人 Codex Desktop Thread 中；
- 群聊页面和 Codex 执行页面可以拥有不同的信息密度和展示方式。

不能存在的差异：

- 已确认的最新需求；
- 当前任务方向；
- 人类最新指令；
- Agent 已完成的关键结果；
- 等待审批、暂停或失败状态；
- 当前由手机、Web 还是 Desktop 持有控制权。

最终目标不是让两端逐字复制所有消息，而是保证两端共享一致的任务状态和必要上下文。

## 8. 建议解决路径

### 阶段 A：验证 Desktop 冷启动恢复行为

先用当前真实 Thread 完成最小实验：

1. 完全关闭 Desktop；
2. 手机发送一轮消息并等待回复完成；
3. 重新启动 Desktop；
4. 打开同一个 Thread；
5. 对照 Web API 检查消息数量、内容和顺序。

如果冷启动可以稳定恢复，短期可以把“重新打开任务”作为明确的手动交接步骤。

### 阶段 B：增加轻量控制权与交接

在统一连接完成前，为同一 Thread 增加单一发送入口约束：

- 手机/Web 接管时，Desktop 不再并发发送；
- 回到电脑前，等待当前 Turn 结束；
- Web 生成一份简短交接状态；
- 释放 Web 控制权；
- 用户重新打开 Desktop 任务继续工作。

交接内容应包含目标、最新决定、当前任务、已完成结果、待确认事项和下一步，不包含无关群聊或完整内部日志。

### 阶段 C：建立统一 Connector 或事件代理

长期应由一个稳定 Connector 负责：

- Thread 的创建和恢复；
- Turn 的发送、中断和补充；
- Notification 分发；
- 当前控制权；
- 任务状态和交接摘要；
- Desktop/Web/手机端的订阅和重连。

所有前端应连接同一个任务事件源，而不是分别启动互不感知的 app-server。

### 阶段 D：多人群与 Codex 上下文映射

项目群完整历史保留在独立协作层。项目经理 Agent 只把已经确认、与任务相关的内容整理为任务包，写入对应 Agent Thread：

```text
项目群讨论
    -> 已确认决定
    -> 任务包
    -> 对应 Agent Thread
    -> 执行结果与状态返回项目群
```

这样既避免群聊污染上下文和浪费 Token，也保证用户回到 Desktop 时能够承接必要信息。

## 9. 不采用的处理方式

- 不直接修改 Codex append-only JSONL；
- 不通过伪造消息让 Desktop 看起来已同步；
- 不把全部多人群聊广播进每个 Agent Thread；
- 不默认创建新的平行 Thread 掩盖同步问题；
- 不在没有协议依据时依赖 Desktop 内部 DOM 强制刷新；
- 不允许 Web 与 Desktop 在没有控制权机制时长期并发写入同一 Thread。

## 10. 临时使用约束

在问题解决前：

1. 同一个 Thread 同一时间只使用手机/Web 或 Desktop 中的一个发送入口；
2. 切回 Desktop 前先等待手机端当前任务完成；
3. 重新打开同一个任务后先核对最新消息是否存在；
4. 如果 Desktop 没有读取到最新内容，继续在 Web 页面工作，不要在旧上下文中并发发送；
5. 不把当前局域网 Demo 宣称为已经完成的无缝多端同步产品。

## 11. 验收标准

完整解决至少需要满足：

1. 手机发送的消息和 Codex 回复可以在 Desktop 中可靠承接；
2. 从 Web 切回 Desktop 不需要用户重新解释已经确认的任务上下文；
3. 两端显示一致的当前任务、最新指令和执行状态；
4. 同一 Thread 不会出现不可解释的并发 Turn 或消息顺序差异；
5. 多人群聊只同步必要任务上下文，不污染 Agent Thread；
6. 断线、关闭和重新打开后能够明确恢复或提示用户采取交接动作；
7. 所有高影响指令仍保留人工授权和审计记录。

## 12. 当前结论

手机/Web 控制真实 Codex 的核心链路已经成立，但 Desktop 与 Web 仍属于“共享持久化 Thread、实时运行实例分离”的状态。已打开的 Desktop 页面不热刷新是已验证问题；Desktop 冷启动后是否能稳定承接 Web 最新内容仍待当前版本实测。

该问题不应通过复制全部消息或修改 JSONL 规避。短期先验证冷启动恢复并增加控制权交接；长期需要统一 Connector 或明确的任务事件代理。多人协作层应保存完整群聊，Codex Thread 只接收项目经理提炼后的必要任务上下文。

## 13. 相关记录

- `DEVELOPMENT_BUG_LOG_2026-07-22.md`：`BUG-20260722-002`，网页写入同一 Thread 后 Desktop 当前页面不热刷新。
- `DEVELOPMENT_LOG_2026-07-22.md`：浏览器双向控制、真实 Thread 写入和 Desktop 同步边界。
- `DEVELOPMENT_LOG_2026-07-24.md`：真实群聊与多 Agent Demo，以及 Desktop/Web 仍非完整双端同步。
- `项目战略与多角色评审/05-多人协作与多Agent技术路径调研报告-2026-07-22.md`：协作层、Connector、Task 和 AgentRun 的推荐分层。

本文件只记录问题和建议方向，不代表同步修复已经实现。
