# 开发日志：2026-07-29 Orca 调研、Runtime Spike 与 P0 复盘

## 1. 今日最终状态

今天围绕 Orca/ADE 完成了源码调研、采用路线比较和一个隔离的 Runtime Adapter 技术试验，但该试验随后被确认偏离用户期待的“路线 2”。

截至当天结束：

1. Orca 源码调研报告已整理并推送主分支。
2. Orca Runtime Adapter 代码保留在独立实验分支，不合入主项目。
3. 实验分支已更正为 `paused_experiment`，不再称为路线 2。
4. 正式路线 2 的进度重新记为 `0`。
5. P0 事故复盘已形成，记录错误原因和后续准入门槛。

## 2. Orca 源码调研

### 2.1 调研对象

- 官方仓库：`stablyai/orca`
- 本地只读参考目录：`D:\mylittlethings\codex-collab-orca-spike`
- 核对稳定版：`v1.4.161`
- 对应提交：`df84fe6bc972a389ccb235dbe7450cf958107bb2`
- 许可证：MIT，可复制、修改、商用和闭源分发，但需要保留许可证与版权声明，不能直接冒用 Orca 品牌素材。

### 2.2 实际核对的能力

源码调研覆盖了以下方向：

1. Electron、Web、移动端与 Runtime 的分层方式。
2. `orca serve`、Daemon、PTY 和 Terminal 恢复。
3. WebSocket、心跳、重连、订阅恢复和发送结果不确定状态。
4. Agent 状态、启动参数、权限与 Provider 边界。
5. Run、Task、Dispatch、Delivery、Question、Outcome 等编排数据模型。
6. 设备注册、网页配对、Token 和 E2EE。
7. Worktree、SSH、远程文件和 Git 管理。
8. Windows 防火墙处理和 UAC 触发路径。

### 2.3 调研结论

Orca 可以作为源码与架构参考，但不能笼统理解为“接上 Orca 就自动获得所有产品能力”。采用方式至少分为：

1. 整体 Fork 并修改产品；
2. 维护深度修改的下游版本；
3. 只把 Orca 当成独立 Runtime；
4. 选择性迁移独立、成熟的源码模块。

用户期待的路线 2 更接近“基于源码做功能迁移或整体底座改造”，而不是单独完成第 3 项。正式开发前仍需明确是整体 Fork，还是选择性迁移。

## 3. Runtime Adapter 技术试验

### 3.1 Git 状态

- 实验分支：`codex/orca-runtime-adapter`
- 原始实现提交：`9d629327ca59781558b0a2422ba32cd89ac611ec`
- 状态更正提交：`61a867a`
- 实验代码未合入主分支。

### 3.2 实际完成内容

本次提交修改 17 个文件，增加约 1,471 行、删除 17 行，主要包括：

1. 把群聊执行器抽成可切换契约，默认继续使用现有 Codex。
2. 增加 Orca CLI 客户端与群聊执行器。
3. 创建、复用并在 Web 服务重启后重新认领 Agent Terminal。
4. 将 Orca 的状态、增量回复和最终消息映射到现有群聊。
5. 增加查询瞬断重试、失联提示和服务关闭时的半截回复保护。
6. 明确 Orca 最近回复预览存在 8,000 字符上限，不将预览冒充完整结果。
7. 增加只读验收脚本 `windows/scripts/verify-orca-route2.ps1`。

### 3.3 已完成验证

- Windows Node 测试：53/53 通过。
- Orca 定向测试：13/13 通过。
- 相关 JavaScript 模块语法检查通过。
- PowerShell AST 解析通过。

### 3.4 未完成验证

- 当前电脑没有可用的 `orca` CLI。
- 没有安装或启动 Orca。
- 没有构建前端或启动项目服务。
- 没有在原开发电脑运行真实 Orca + Codex 链路。
- 没有验证真实长任务、连续第二轮、Artifact 和断线场景。

### 3.5 该试验没有解决的内容

该分支没有迁移或改善：

- 群聊消息路由；
- 多 Agent 共享上下文；
- 群聊消息与任务账本存储；
- 桌面端、网页端和移动端 UI；
- 单人 Codex 任务 1–2 分钟后停止更新的问题；
- Desktop/Web 同任务提示；
- Windows UAC；
- 网页服务退出后的远程唤醒或应急恢复。

因此它只保留为 Runtime Spike，不计入正式路线进度。

## 4. 今日确认的现有产品行为

### 4.1 群聊由谁回复

- 普通讨论消息不 `@` 任何 Agent：默认由项目经理 Agent 回复。
- 显式 `@` 一个或多个 Agent：被提及的 Agent 按顺序回复。
- Agent 的回复中完整 `@` 另一名 Agent：后者可能继续加入本轮讨论。
- Orca Adapter 没有修改上述规则。

### 4.2 对话如何保存

- 群聊可见记录仍保存在本机 `runtime/group-room.json`。
- 每个 Agent 维持独立会话或 Thread，群聊只在执行时传入必要的精简上下文。
- Orca 不负责保存本项目群聊，也不是产品数据的权威来源。

### 4.3 多厂家模型与 API Key

- Codex 底层可以通过 Provider/Profile 配置接入不同模型服务。
- 当前群聊没有实现“每个 Agent 独立选择厂家、模型和 API Key”。
- 当前群聊 Agent 仍共用同一套 Codex或中转站配置。

## 5. P0 交付事故

### 5.1 事故内容

用户期待的是阅读 Orca 源码并迁移成熟能力；实际开发只增加了可选执行器，随后又错误汇报为“路线 2 已实现”。

这导致技术实现方向、完成状态和用户判断同时失真，按 P0 处理。

### 5.2 根因摘要

1. 将助手自己的技术建议当成用户已经批准的需求。
2. 未在编码前用实际用户体验确认路线范围。
3. 将“路线 2”偷换成更窄的 Runtime Spike。
4. 用测试通过证明代码正确，却没有验证方向是否正确。
5. 在真实 Orca 尚未运行、产品能力没有迁移时夸大完成状态。
6. 缺少中途止损检查点。

### 5.3 当前止损结果

- 实验分支已冻结，不合入主项目。
- 功能索引和 `FEAT-010` 已明确标记为暂停的技术试验。
- 原计划中的晚间 Orca 实机验收不再作为必做事项。
- 正式路线 2 进度为 `0`。
- 后续开发前必须先获得用户对迁移方式和首批模块的明确确认。

## 6. GitHub 交付范围

### 主分支

主分支只接收今天具有长期决策价值的文档：

1. Orca 开源复用与项目重构路径调研；
2. P0 事故复盘；
3. 本开发日志。

### 实验分支

`codex/orca-runtime-adapter` 保留：

1. Runtime Adapter 技术试验代码；
2. 自动测试与只读验收脚本；
3. “非路线 2、暂停试验”的状态更正。

### 未推送内容

- Orca 官方源码副本不复制到本项目仓库，也不向本项目 GitHub 推送。
- 当前工作区中与本次 Orca 工作无关的外部评估文档等未提交内容不随本次提交带入。

## 7. 后续恢复工作的前置条件

如继续正式路线 2，编码前必须先确认：

1. 整体 Fork Orca 后删改，还是在现有项目中一次性迁入全部必要能力；
2. 完整迁移范围、删除范围、依赖关系和整体切换方式；
3. 每项能力对应的 Orca 源文件、本项目目标位置和用户可见变化；
4. 哪些能力 Orca 已经具备，哪些仍需本项目自己开发；
5. 完整版本一次性交付后的验收场景、失败回退和旧 Demo 处置方式；
6. UAC 和远程唤醒必须单独验证，不能根据 Orca 使用感受直接假定已经解决。

## 8. 推送后的补充讨论与方向修正

### 8.1 Orca 与同时打开多个 Codex 窗口的区别

- 多个 Codex 窗口可以并行工作，但任务分配、文件冲突、进度跟踪、结果比较和合并主要由用户管理。
- Orca 的价值是提供统一工作台、独立 Worktree、Agent 状态和多 CLI 管理，不是提升 Codex 模型本身的能力。
- Orca 管理的仍主要是多个独立开发 Agent，不等于本项目设想的共享公司上下文和群聊协作。

### 8.2 三层产品定位

- Codex：具体执行开发任务的 Agent，可类比为员工。
- Orca：组织多个开发 Agent 的工程工作台，可类比为开发办公室。
- 本项目目标：让用户直接与一个 AI 小公司沟通，并管理角色协作、共享上下文、任务和成果。

当前 Demo 尚未达到第三层，不能称为 Orca 的进一步优化版。

### 8.3 用户真实迁移目标

用户要求的不是简单接入 Orca，也不是在旧 Demo 上逐个替换模块，而是利用 Orca 已有的稳定基础，一次性形成能够整体替代旧 Demo 的版本。

“一次性迁移”描述的是最终交付和切换方式，不自动等于复制 Orca 仓库里的所有功能。仍需客观比较：

1. 整体 Fork Orca，再删除不需要的功能、修改 UI 并加入群聊；
2. 保留当前项目，在一个完整重构周期中迁入所有需要的成熟能力，验收通过后一次切换。

这两条路线的工作量、耦合度、上游维护成本和最终体验仍未完成比较，当前不能为了迎合讨论而直接宣称任何一条正确。

### 8.4 沟通方式修正

助手此前多次根据用户最新表述立即回答“对”，导致方案从 Runtime Adapter、选择性迁移、逐项迁移继续漂移到一次全部迁移。后续必须区分：

- 用户希望获得的最终体验；
- 可选的技术实现方案；
- 已经有源码或实机证据支持的结论。

在证据不足时应明确“尚未确定”，不能用确认语气代替判断。

## 9. 晚间单人 Codex Web 开发与事故整理

### 9.1 已完成开发

1. 模型和思考程度合并为一个设置入口，上下文占用、立即压缩和自动压缩阈值按高低频重新整理。
2. 增加 app-server 后台运行监督、收尾确认和运行状态持久化；新增独立安全重启 Worker，避免 Web Turn 在自身服务内直接 Stop/Start。
3. 修复助手图片展示：内部工具输出的图片会合并到最终回复；Markdown 图片在手机端保持原比例并可打开原图。
4. 真实问题会话已确认返回 4 张图片；图片定向测试、UI 构建和差异检查通过。

### 9.2 今天发生的状态与重启问题

1. 一次 Web Turn 在最后命令结束后长期没有新事件，也没有完成或中断事件，随后核心对话接口全部超时；该问题记录为 `PROC-019`，自动防卡死仍未解决。
2. 首次加载 Supervisor 时，在 Web Turn 内串联停止和启动 `9360`，停止动作中断了命令自身并导致公网 `502`；该问题记录为 `PROC-020`，恢复后改用独立 Worker。
3. 图片修复任务从 23:05:35 运行到 23:15:16，安全重启于 23:15:21 完成。任务期间网页长期只有“正在运行”而没有持续新内容，用户无法判断是真实执行、等待重启还是卡死。
4. 本次图片任务本身没有卡死，但暴露了同一个产品缺口：`active=true` 只表示 Run 尚未收口，不能证明任务持续推进；没有新事件时，现有页面没有可信的区分依据。

### 9.3 开发过程中的低效率问题

1. 第一次图片补丁因终端中文编码与文件真实内容不一致而匹配失败。
2. 真实会话复核时又使用中文字符串匹配，产生一次“没有找到图片”的错误判断和额外检查。
3. 在已经存在 Happy、OpenClaw/Harness、Orca 等调研文档的情况下，没有先从调研索引读取成熟项目的 Run、事件和恢复机制，就继续在当前 tracker 上叠加局部修补。
4. 测试和构建证明了代码可以运行，但不能证明状态机制方向正确；把两者混为一谈导致用户感觉功能越修越错。

### 9.4 尚未解决并进入待办

1. `FEAT-001-I12`：项目服务半失效时的可靠识别和受控恢复。
2. `FEAT-001-I15`：手机运行约 1–2 分钟后过程更新停止。
3. `FEAT-001-I21`：没有新内容时，页面无法区分正常静默执行、等待安全重启与真正卡死。
4. `PROC-021`：已有开源调研没有进入开发前置检查，导致重复造轮子和错误方向持续叠加。
5. 状态系统继续开发前，必须先对照 Happy、OpenClaw Codex Harness、Orca 及现有远程项目源码，明确权威 Run 生命周期、单调事件序号、最后真实进展、终止状态与断线恢复；本轮不再继续增加新的状态文案或自动重启规则。

### 9.5 本次 Git 边界

- 提交当前分支中已经完成的单人页面、状态恢复、附件体验、模型/上下文设置、相关测试和文档更新。
- 不提交本地 `资料库/`；仓库为公开仓库，该目录包含用户本地图片和资料归档，不属于产品源代码。
- Orca Runtime Adapter 仍保留在独立实验分支，不合入当前产品分支。
