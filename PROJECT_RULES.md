# 项目规则

本文件是项目协作、开发、文档和审计的统一入口。新规则以本文件为准，历史记录不因本文件而改写。

## 1. 分支规则

- `main`：稳定版本，只接收已经验证的改动。
- `codex/publish-current-panel`：当前开发版本，代码和项目文档放在同一条提交链上。
- 临时审计或实验使用临时 worktree；交付后删除，不保留长期分支。
- 不再维护独立的长期文档分支。

## 2. 文档规则

- 新需求、研究、审计、开发记录统一放在 `docs/`。
- 需求记录说明目标、当前体验、预期体验和验收标准。
- 研究记录说明来源、已确认事实、推测和未知项。
- 审计记录必须写明审计所基于的产品 commit SHA。
- 开发记录说明改动范围、测试结果、未验证风险和下一步。
- 历史根目录日志暂不强行移动；新记录不再散落到根目录。

## 3. 开发流程

1. 先确认需求编号、目标体验和改动边界。
2. 在 `codex/publish-current-panel` 开发，不顺手扩大功能范围。
3. 代码改动和受影响的文档在同一里程碑提交中完成。
4. 运行与改动相关的定向测试、构建检查和 `git diff --check`。
5. 提交后记录完整 SHA、用户可见变化、已验证内容和未验证问题。

## 4. 审计规则

- 审计必须从当前开发分支的准确 commit 创建 worktree。
- 审计先提交文档或测试，不直接修改生产逻辑。
- 审计报告必须区分：已确认事实、推测、未知问题和建议。
- 只有确认范围和测试边界后，才进行文件拆分或协议调整。

## 5. 合入规则

合入 `main` 前必须确认：

- 代码已完成对应验证；
- 受影响需求和开发记录已更新；
- 审计结论没有被新代码覆盖；
- 工作区干净，提交内容只包含本次任务范围。

## 6. 安全和范围规则

- 不擅自启动、重启服务或执行远程危险操作。
- 不把 UI 重构、群聊扩展、UAC 处理等无关事项混入当前补丁。
- 不覆盖用户已有未提交改动。
- 删除分支、worktree 或重要文件前，先确认内容已经迁移并可恢复。

## 7. 文档命名规则

新文档统一使用主题、类型和日期命名：

```text
docs/research/<主题>_RESEARCH_YYYY-MM-DD.md
docs/research/<主题>_AUDIT_YYYY-MM-DD.md
docs/records/<主题>_DEVLOG_YYYY-MM-DD.md
docs/records/<主题>_INCIDENT_YYYY-MM-DD.md
```

命名要求：

- `<主题>` 使用简短、稳定、可检索的英文大写单词和下划线。
- 日期使用 `YYYY-MM-DD`，例如 `2026-08-03`。
- 文件头必须记录 `document_type`、`title`、`date`、`product_commit` 和 `status`。
- 研究、审计和开发记录必须绑定产品开发分支的完整 commit SHA。
- 同一主题的新版本使用新日期文件，不覆盖历史结果。

## 8. 项目管理同步规则

- 有独立任务编号的需求、研究或事故，在 `docs/project-management/items/<ITEM-ID>/` 建立条目。
- `item.md` 保存当前状态，`updates.md` 只追加里程碑，`process.md` 只记录该条目的过程问题。
- 项目管理条目的 `source` 必须指向对应的需求、研究或记录文件。
- 项目管理页面读取条目文件，不根据分支名称或聊天内容推断状态。
