# 项目基本运行规范

版本：1.0
用途：为不同项目提供一套简短、可复用的开发、命名和项目管理同步规则。

本文件只规定“项目如何运行和交接”，不规定具体产品方向、技术实现或研究结论。项目自身的分支、安全、平台和历史资料规则，写在项目专属的 `PROJECT_RULES.md`。

## 1. 资料分层

| 层级 | 负责内容 | 主要位置 |
| --- | --- | --- |
| 运营规范 | 开发流程、命名、交接、项目管理面板同步 | `PROJECT_OPERATING_RULES.md`、`PROJECT_RULES.md` |
| 功能开发 | 需求、当前体验、目标体验、问题和版本记录 | `docs/feature-development/` |
| 技术架构 | 文件职责、调用边界、依赖方向、架构审计 | `docs/architecture/` |
| 研究资料 | 技术调研、竞品、产品和外部资料 | `docs/research/` 或项目指定的研究资料目录 |
| 运行记录 | 开发日志、验证记录、事故复盘 | `docs/records/` |
| 项目管理数据 | 条目状态、里程碑和过程记录 | `docs/project-management/` |

功能状态以功能记录和项目管理条目为准；架构目录和研究资料不直接代表当前开发状态。

## 2. 开发规则

1. 开始前确认任务编号、范围、目标体验和产品基线 commit。
2. 只在指定开发分支完成本轮范围内的最小改动，不把无关功能混入同一补丁。
3. 代码改动完成后运行与本次改动相关的测试、构建或检查。
4. 提交必须说明修改范围、完整 commit SHA、用户可见变化、已验证内容和未验证风险。
5. 交接前确认工作区状态、远程推送状态和项目管理条目已经同步。

## 3. 命名规则

- 功能：`FEAT-<编号>-<短名称>.md`。
- 研究：`<主题>_RESEARCH_YYYY-MM-DD.md`。
- 审计：`<主题>_AUDIT_YYYY-MM-DD.md`；架构审计可放在 `docs/architecture/audits/`。
- 开发日志：`<主题>_DEVLOG_YYYY-MM-DD.md`。
- 事故复盘：`<主题>_INCIDENT_YYYY-MM-DD.md`。
- 项目条目：`docs/project-management/items/<ITEM-ID>/{item,updates,process}.md`。
- 文件名使用稳定、可检索的英文主题和日期；历史文件不覆盖，使用新日期建立新版本。

## 4. 项目管理面板同步

- `item.md`：只保存条目的当前快照、状态、负责人、来源和 commit 关联。
- `updates.md`：只追加可核对的里程碑、用户影响、测试结果和下一步。
- `process.md`：只记录该条目的弯路、阻塞和决策；可复用经验再登记到通用问题文档。
- 面板只读取条目文件，不根据分支名、聊天内容或文件时间推断状态。
- 没有任务编号、完整 SHA、测试结果或条目回写时，交接状态只能是 `handoff_pending`。

## 5. 轻量任务归档

本节是跨项目可复用规则，不规定具体项目目录。

- 开始修改、研究或提交前，先定位或建立任务编号；普通闲聊不自动创建任务。
- 一个逻辑步骤可以批量读取相关资料并在阶段结束时汇报，不要求一个文件一个步骤。
- 工作结束时只更新必要记录：当前状态写入 `item.md`，进展追加到 `updates.md`，条目专属弯路写入 `process.md`，可复用经验才进入通用问题文档。
- 交接至少说明修改文件、用户可见变化、已运行检查、未验证风险和下一步；不以完整聊天记录代替项目记录。

## 6. 完成定义

一次工作只有同时满足“范围完成、检查完成、提交可核对、项目管理已同步、工作区状态已说明”时，才可以描述为已完成。
